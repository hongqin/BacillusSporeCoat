library(ape);
t = read.tree("BG10012-bacillus.nwk");
t
plot(t);
str(t);
plot(t);
 tb = read.table("_ids.bacillus.csv", sep="\t");
tb
 tb = read.table("_ids.bacillus.csv", sep="\t");
 names(tb) = c("phylip","original");
tb
str(tb);
 tb = as.character(tb);
str(tb);
tb
 tb = read.table("_ids.bacillus.csv", sep="\t");
 names(tb) = c("phylip","original");
 tb$phylip = as.character(tb$phylip);
tb
str(tb);
 tb = read.table("_ids.bacillus.csv", sep="\t");
 names(tb) = c("phylip","original");
 tb$phylip = as.character(tb$phylip);
 tb$original = as.character(tb$original);
tb
t
str(t);
   hits = tb$original[ tb$phylip == t$tip.label ]
hits
tb$phylip
t$tip.label
tb$phylip == t$tip.label;
?match
tb$phylip %in% t$tip.label
  hits = tb$original[ match(t$tip.label,tb$phylip) ]
htis
hits
cbind(nodes,hits);
 nodes = t$tip.label;
 hits = tb$original[ match(t$tip.label,tb$phylip) ]
 cbind(nodes,hits);
 nodes = t$tip.label;
 hits = tb$original[ match(t$tip.label,tb$phylip) ]
 cbind(nodes,hits); #check, OK
 t$tip.label = hits;
 plot(t)
